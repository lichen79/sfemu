[package]
name = "machine"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

# One dependency, ever. `machine` must not gain `romset`: that would drag in
# miniz_oxide and std and forfeit the WASM posture sub-project A paid for.
[dependencies]
m68k = { path = "../m68k" }
